name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout code (local only, SSH server will clone again)
    - name: Checkout repository
      uses: actions/checkout@v3

    # 2. Deploy inside Azure VM
    - name: Deploy to Azure VM
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.AZURE_VM_IP }}
        username: ${{ secrets.AZURE_VM_USER }}
        key: ${{ secrets.AZURE_VM_SSH_KEY }}
        script: |
          echo "Pulling latest code from public GitHub repo..."

          # If folder doesn't exist, clone fresh
          if [ ! -d "/home/${{ secrets.AZURE_VM_USER }}/project" ]; then
            git clone https://github.com/MiniduOshan/AcademicPro.git project
          fi

          cd project

          # Pull latest changes
          git reset --hard HEAD
          git pull origin main

          echo "Building Frontend Docker image..."
          docker build -t academicpro-frontend ./frontend

          echo "Building Backend Docker image..."
          docker build -t academicpro-backend ./backend

          echo "Stopping old containers..."
          docker stop academicpro-frontend || true
          docker stop academicpro-backend || true

          echo "Removing old containers..."
          docker rm academicpro-frontend || true
          docker rm academicpro-backend || true

          echo "Starting new backend..."
          docker run -d --name academicpro-backend -p 5000:5000 academicpro-backend

          echo "Starting new frontend..."
          docker run -d --name academicpro-frontend -p 80:80 academicpro-frontend

          echo "Deployment completed successfully!"
